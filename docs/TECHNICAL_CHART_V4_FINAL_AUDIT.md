# Technical Chart V4 final audit ledger

Audit base: `edd5236de927f109893d797830d0c116c4f9d3f6`  
Audit branch: `fix/technical-chart-v4-final-audit`

| ID | Severity | Area | Root cause and user impact | Corrective change | Regression evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| V4-AUD-001 | BLOCKER | Liquidity / no-lookahead | A liquidity zone was built from every matching future swing. Appending a later equal high/low moved the original zone's `availableAt`, retroactively changing when a historical signal existed. | Freeze zone identity, bounds and availability when the first confirmed pair forms; later observations may only evolve lifecycle state. | `technical-v4.final-audit.test.ts`: immutable liquidity-zone identity across prefix/full datasets, ATR boundary and sweep confirmation fixtures. Pre-fix regression fixture failed because availability moved from 2024-01-11 to 2024-01-25; post-fix passes. | RESOLVED |
| V4-AUD-002 | HIGH | Position planner | Explicit zero/negative account size or risk percentage was converted to absent sizing while the plan remained `VALID`; a zero-reward target also produced a valid-looking plan. | Reject explicitly invalid sizing inputs, reject numerical overflow, and keep a directionally valid plan `INCOMPLETE` until it has at least one valid target. | Independent LONG/SHORT, T1/T2/T3, R multiple, zero-risk, invalid direction, fractional crypto and large-number fixtures. | RESOLVED |
| V4-AUD-003 | HIGH | Historical range resilience | Range selection immediately changed the dataset key. During a failed longer-history request the previous verified chart was no longer selected, so the panel could become empty despite a valid snapshot being available. | Retain the latest verified dataset for the same symbol/timeframe while an exact range request loads or fails, with an explicit inline status. | Playwright fault-injection scenario verifies that a failed MSFT MAX request leaves the prior chart visible and labels it as the last verified snapshot. | RESOLVED |
| V4-AUD-004 | MEDIUM | Cross-asset volatility | Realized volatility always used `sqrt(252)`, including crypto and intraday data, so absolute volatility was understated for 24/7 assets and mis-scaled intraday. | Select observation periods from asset class and timeframe, expose the basis, and document square-root-of-observations-per-year scaling. | Known-return fixture verifies beta, correlation, missing-date alignment and `sqrt(365)` daily crypto volatility. | RESOLVED |
| V4-AUD-005 | MEDIUM | Custom history | Client, storage and API validation rejected `FROM = TO`, preventing a valid one-session or one-day historical request. | Accept equal ISO dates end-to-end, cap the effective end at the current instant, and continue rejecting reversed/future-only ranges. | Engine and schema fixtures verify same-day, reversed and future-only behavior. | RESOLVED |
| V4-AUD-006 | HIGH | Cross-asset live provider alignment | The initial Preview showed NVDA and QQQ each with 252 verified 1Y bars, yet cross-asset context was `UNAVAILABLE`. Their providers timestamped the same trading dates at different UTC hours, while the engine required byte-identical timestamps. | Align daily bars by UTC session date and weekly bars by UTC ISO week; retain exact timestamp alignment for intraday. The paired observation timestamp is the later source timestamp. No missing session is fabricated. | Daily/weekly shifted-provider and intraday non-overlap fixtures pass. Preview showed 252 verified NVDA bars and an available QQQ context with beta and 20/60 correlations. | RESOLVED; PREVIEW VERIFIED |
| V4-AUD-007 | BLOCKER | Live MAX history coverage | The post-fix Preview accepted Massive's successful but plan-limited response for NVDA `MAX`, exposing only 501 daily bars since September 2024 instead of the available history from 1999. | Route `3Y`, `5Y`, `10Y` and `MAX` through coverage-aware provider selection. Continue to the next configured provider when the first valid response does not span 90% of the requested years; retain the deepest verified response when an instrument genuinely has less history. | Provider-selection and Technical-service regression fixtures plus post-fix Preview acceptance are required. | FIXED LOCALLY; PREVIEW PENDING |

The first corrective pass was limited to the five pre-existing findings. The sixth and seventh findings were discovered during real Preview acceptance after P0 reconciliation. Their changes are confined to V4 cross-asset session alignment and long-history provider coverage. They do not change Pattern, Seasonality, Analysis, Calendar, Search or authentication.

## P0 reconciliation

The audit branch incorporates production main `fd5d7eed4ff4f9b4dfc95c862c52144bf251d7db` via merge `26c1a0f`, after preserving the five corrections in `8bda446`. The merge had zero textual conflicts. Its P0 changes are limited to the authenticated dashboard bootstrap, persistent watchlist mutations, watchlist search and canonical BTC/ETH identity; none overwrote a V4 calculation or chart file.

| Prior issue | Before reconciliation | After reconciliation |
| --- | --- | --- |
| V4-AUD-001 · BLOCKER | Resolved by immutable first-pair liquidity geometry and availability. | Regression fixture passes; no-lookahead behavior retained. |
| V4-AUD-002 · HIGH | Resolved by explicit sizing validation and no-target incomplete status. | LONG/SHORT, zero-input and overflow fixtures pass. |
| V4-AUD-003 · HIGH | Resolved by same-symbol/timeframe verified chart fallback. | Fault-injected MAX browser test passes with prior chart retained. |
| V4-AUD-004 · MEDIUM | Resolved by asset/timeframe-specific volatility annualization. | Known-return crypto and equity fixtures pass. |
| V4-AUD-005 · MEDIUM | Resolved by equal-date custom-range support. | Same-day unit and browser tests pass. |

Targeted reconciliation evidence: 70/70 auth, watchlist, search, identity, workspace and V4 engine tests; 10/10 desktop P0/V4 browser tests; authenticated login → NVDA watchlist → Technical MAX → BTC/ETH search → ETH Technical → logout/login browser flow passes (2/2 including its warmup). Deterministic E2E provider fixtures are enabled. A warm run of the V4 structure engine measured mean/p95/max of 4.2/4.7/4.7 ms at 1,000 bars, 36.7/43.1/43.1 ms at 5,000 bars, and 111.9/118.3/118.3 ms at 10,000 bars, across ten samples after three warmups on the local machine. These are below the V4 reference means of approximately 8/39/131 ms; the harness and fixture may differ, so they are a regression signal rather than a hardware-independent guarantee.

Final local gates after reconciliation: lint passed; TypeScript passed; 655/655 unit and integration tests passed; Next.js production build passed; 154/154 production-mode browser scenarios passed across desktop, tablet and mobile; tracked-file and client-bundle secret scan found no credentials or embedded local environment values. The TypeScript gate initially encountered duplicate numbered copies of generated `.next/dev/types` declarations. `tsconfig.json` now excludes all filename copies with a space before the extension, without deleting those generated files or the preserved untracked ` 2` files.

After resolving V4-AUD-006, the final local gates were repeated: lint passed; TypeScript passed; 656/656 unit and integration tests passed; Next.js production build passed; 154/154 production-mode browser scenarios passed across desktop, tablet and mobile; the tracked-file and client-bundle secret scan again found no credentials or embedded local environment values. Real Preview acceptance remains a separate gate.

After resolving V4-AUD-007, the complete local gates were repeated again: lint passed; TypeScript passed; 664/664 unit and integration tests passed; Next.js production build passed; 154/154 production-mode browser scenarios passed across desktop, tablet and mobile; the tracked-file and client-bundle secret scan found no credentials or embedded local environment values. Post-fix Preview coverage and the full authenticated observation remain separate gates.
