export interface AccountUser { id: string; email: string; name: string | null; role: "USER" | "ADMIN"; }
export interface AccountWatchlistItem { id: string; symbol: string; name: string; type: string; currency: string | null; market: string | null; position: number; notes: string | null; price: number | null; changePercent: number | null; volume: number | null; marketState: string | null; lastUpdated: string | null; provider: string | null; signal: string | null; confidence: number | null; target: number | null; nextEvent: { title: string; startsAt: string } | null; activeAlerts: number; }
export interface AccountWatchlist { id: string; name: string; description: string | null; items: AccountWatchlistItem[]; }
export interface AccountPortfolioPosition { instrumentId: string; symbol: string; name: string; quantity: number; averagePrice: number; realizedPnl: number; currentPrice: number | null; currency: string | null; marketValue: number | null; unrealizedPnl: number | null; allocation: number | null; lastUpdated: string | null; provider: string | null; }
export interface AccountPortfolio { id: string; name: string; baseCurrency: string; positions: AccountPortfolioPosition[]; totalMarketValue: number; realizedPnl: number; unrealizedPnl: number; concentration: number; warnings: string[]; }
export interface AccountAlert { id: string; type: string; status: string; symbol: string | null; configuration: Record<string, unknown>; lastEvaluatedAt: string | null; triggeredAt: string | null; expiresAt: string | null; createdAt: string; }
export interface AccountNotification { id: string; alertId: string; type: string; symbol: string | null; payload: Record<string, unknown>; deliveredAt: string | null; createdAt: string; }
